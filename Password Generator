import secrets
import string

extent = secrets.choice(range(5,15))

characters = string.ascii_letters  + string.digits + string.punctuation  

password = "".join(secrets.choice(characters) for i in range(extent))
print(password)
